---
phase: 05.2-ui-review-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/tailwind.config.ts
  - packages/web/components.json
  - packages/web/src/lib/utils.ts
  - packages/web/src/components/theme-provider.tsx
  - packages/web/src/components/theme-toggle.tsx
  - packages/web/src/app/layout.tsx
  - packages/web/src/app/page.tsx
  - packages/web/src/components/error-boundary.tsx
  - packages/web/src/components/conflict-modal.tsx
  - packages/web/src/components/presence-badge.tsx
autonomous: true
requirements:
  - UI-POLISH-01
  - UI-POLISH-02
  - UI-POLISH-03

must_haves:
  truths:
    - "Dark mode renders by default on first visit with no flash of white"
    - "ThemeToggle in nav cycles system->dark->light and persists across refresh"
    - "Home page / redirects to /reservations immediately"
    - "Nav bar has visible bottom border separating it from content"
    - "ErrorBoundary, ConflictModal, and PresenceBadge use design tokens not hardcoded colors"
  artifacts:
    - path: "packages/web/tailwind.config.ts"
      provides: "darkMode class strategy, status color tokens, tailwindcss-animate plugin"
      contains: "darkMode"
    - path: "packages/web/components.json"
      provides: "shadcn CLI configuration"
      contains: "shadcn"
    - path: "packages/web/src/lib/utils.ts"
      provides: "cn() utility for shadcn components"
      exports: ["cn"]
    - path: "packages/web/src/components/theme-provider.tsx"
      provides: "next-themes ThemeProvider wrapper"
      exports: ["ThemeProvider"]
    - path: "packages/web/src/components/theme-toggle.tsx"
      provides: "ThemeToggle icon button component"
      exports: ["ThemeToggle"]
    - path: "packages/web/src/app/page.tsx"
      provides: "Redirect to /reservations"
      contains: "redirect"
  key_links:
    - from: "packages/web/src/app/layout.tsx"
      to: "packages/web/src/components/theme-provider.tsx"
      via: "ThemeProvider wrapping body content"
      pattern: "ThemeProvider"
    - from: "packages/web/src/app/layout.tsx"
      to: "packages/web/src/components/theme-toggle.tsx"
      via: "ThemeToggle rendered in nav header"
      pattern: "ThemeToggle"
---

<objective>
Set up dark mode infrastructure (next-themes, Tailwind config, ThemeProvider, ThemeToggle), initialize shadcn CLI, fix the home page redirect, add the nav border, and fix hardcoded color tokens on standalone components (ErrorBoundary, ConflictModal, PresenceBadge).

Purpose: Establishes the foundation that all subsequent plans depend on -- dark mode works, shadcn components can be installed, design token violations on non-reservation components are eliminated.
Output: Working dark mode with toggle, shadcn CLI ready, home page redirects, 3 components tokenized.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.2-ui-review-polish/05.2-CONTEXT.md
@.planning/phases/05.2-ui-review-polish/05.2-UI-SPEC.md
@.planning/phases/05.2-ui-review-polish/05.2-RESEARCH.md

<interfaces>
From packages/web/tailwind.config.ts (current state -- to be modified):
- Missing: darkMode config, status color tokens, tailwindcss-animate plugin
- Has: CSS variable color tokens for background, foreground, primary, etc.

From packages/ui/tailwind.config.ts (reference for status tokens):
```typescript
status: {
  confirmed: 'hsl(142 76% 36%)',
  pending: 'hsl(38 92% 50%)',
  seated: 'hsl(217 91% 60%)',
  completed: 'hsl(215 20% 65%)',
  cancelled: 'hsl(0 84% 60%)',
  'no-show': 'hsl(0 0% 45%)',
},
```

From packages/web/src/app/globals.css (complete -- dark mode CSS vars already defined):
- :root block with light mode HSL values
- .dark block with dark mode HSL values
- No changes needed to globals.css
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tailwind config, shadcn CLI init, dependency installation</name>
  <files>packages/web/tailwind.config.ts, packages/web/components.json, packages/web/src/lib/utils.ts</files>
  <read_first>
    - packages/web/tailwind.config.ts
    - packages/ui/tailwind.config.ts
    - packages/web/package.json
    - .planning/phases/05.2-ui-review-polish/05.2-RESEARCH.md (Pattern 1: shadcn CLI Initialization, Tailwind Config Updates section)
  </read_first>
  <action>
1. Install dependencies in packages/web:
   ```bash
   cd packages/web && pnpm add next-themes motion sonner && pnpm add -D tailwindcss-animate clsx tailwind-merge
   ```

2. Update `packages/web/tailwind.config.ts`:
   - Add `darkMode: ['class']` at top level (per D-04)
   - Add status color tokens under `theme.extend.colors.status` matching the exact values from `packages/ui/tailwind.config.ts`:
     ```
     status: {
       confirmed: 'hsl(142 76% 36%)',
       pending: 'hsl(38 92% 50%)',
       seated: 'hsl(217 91% 60%)',
       completed: 'hsl(215 20% 65%)',
       cancelled: 'hsl(0 84% 60%)',
       'no-show': 'hsl(0 0% 45%)',
     }
     ```
   - Add `import animate from 'tailwindcss-animate'` at top
   - Add `plugins: [animate]` (replace empty plugins array)

3. Create `packages/web/components.json` for shadcn CLI:
   ```json
   {
     "$schema": "https://ui.shadcn.com/schema.json",
     "style": "default",
     "rsc": true,
     "tsx": true,
     "tailwind": {
       "config": "tailwind.config.ts",
       "css": "src/app/globals.css",
       "baseColor": "slate",
       "cssVariables": true,
       "prefix": ""
     },
     "aliases": {
       "components": "@/components",
       "utils": "@/lib/utils",
       "ui": "@/components/ui",
       "lib": "@/lib",
       "hooks": "@/hooks"
     }
   }
   ```

4. Create `packages/web/src/lib/utils.ts` with standalone cn() utility:
   ```typescript
   import { clsx, type ClassValue } from 'clsx';
   import { twMerge } from 'tailwind-merge';

   export function cn(...inputs: ClassValue[]) {
     return twMerge(clsx(inputs));
   }
   ```

5. Run `cd packages/web && npx shadcn add sheet dialog skeleton tabs sonner --yes` to install all shadcn components at once.

6. Verify installation: `ls packages/web/src/components/ui/` should show sheet.tsx, dialog.tsx, skeleton.tsx, tabs.tsx, sonner.tsx.
  </action>
  <verify>
    <automated>cd /workspace/packages/web && ls src/components/ui/sheet.tsx src/components/ui/dialog.tsx src/components/ui/skeleton.tsx src/components/ui/tabs.tsx src/components/ui/sonner.tsx src/lib/utils.ts components.json && grep -q "darkMode" tailwind.config.ts && grep -q "status" tailwind.config.ts && grep -q "animate" tailwind.config.ts && pnpm exec tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - packages/web/tailwind.config.ts contains `darkMode: ['class']`
    - packages/web/tailwind.config.ts contains `status:` with `confirmed`, `pending`, `seated`, `completed`, `cancelled`, `no-show` keys
    - packages/web/tailwind.config.ts contains `plugins: [animate]` or equivalent
    - packages/web/components.json exists with `"style": "default"` and `"ui": "@/components/ui"`
    - packages/web/src/lib/utils.ts exports `cn` function
    - packages/web/src/components/ui/sheet.tsx exists
    - packages/web/src/components/ui/dialog.tsx exists
    - packages/web/src/components/ui/skeleton.tsx exists
    - packages/web/src/components/ui/tabs.tsx exists
    - packages/web/src/components/ui/sonner.tsx exists
    - packages/web/package.json contains "next-themes", "motion", "sonner", "tailwindcss-animate"
    - TypeScript compilation produces no errors in packages/web
  </acceptance_criteria>
  <done>All dependencies installed, Tailwind config updated with darkMode + status tokens + animate plugin, shadcn CLI initialized, all 5 shadcn components installed, cn() utility created.</done>
</task>

<task type="auto">
  <name>Task 2: Dark mode infrastructure + ThemeToggle + home page redirect + nav border</name>
  <files>packages/web/src/components/theme-provider.tsx, packages/web/src/components/theme-toggle.tsx, packages/web/src/app/layout.tsx, packages/web/src/app/page.tsx</files>
  <read_first>
    - packages/web/src/app/layout.tsx
    - packages/web/src/app/page.tsx
    - packages/web/src/components/ui/sonner.tsx
    - .planning/phases/05.2-ui-review-polish/05.2-RESEARCH.md (Pattern 2: next-themes ThemeProvider, Pattern 3: ThemeToggle)
    - DESIGN.md (Section 4: Navigation styling, Section 7: Do's - synchronous script)
  </read_first>
  <action>
1. Create `packages/web/src/components/theme-provider.tsx` (per D-02):
   ```typescript
   'use client';

   import { ThemeProvider as NextThemesProvider } from 'next-themes';
   import type { ComponentProps } from 'react';

   export function ThemeProvider({
     children,
     ...props
   }: ComponentProps<typeof NextThemesProvider>) {
     return <NextThemesProvider {...props}>{children}</NextThemesProvider>;
   }
   ```

2. Create `packages/web/src/components/theme-toggle.tsx` (per D-03):
   - Import Monitor, Moon, Sun from lucide-react
   - Import useTheme from next-themes
   - Add `mounted` state guard (useEffect sets mounted=true) to avoid hydration mismatch
   - Before mounted, render a placeholder `<div className="w-8 h-8" />` (same dimensions)
   - Cycle: system -> dark -> light -> system
   - Icon: Monitor for system, Moon for dark, Sun for light
   - aria-label: `Switch to ${nextTheme} mode`
   - Button classes: `inline-flex items-center justify-center w-8 h-8 rounded-md hover:bg-muted focus:outline-none focus:ring-2 focus:ring-ring focus:ring-offset-2`
   - Export named `ThemeToggle`

3. Rewrite `packages/web/src/app/layout.tsx` (per D-01, D-02, D-03, D-12, F-02, F-03):
   - Add `suppressHydrationWarning` to `<html>` tag (required by next-themes)
   - Import ThemeProvider from `@/components/theme-provider`
   - Import ThemeToggle from `@/components/theme-toggle`
   - Import Toaster from `@/components/ui/sonner`
   - Wrap body content with `<ThemeProvider attribute="class" defaultTheme="dark" enableSystem disableTransitionOnChange storageKey="seatkit-theme">`
   - Update header classes to: `h-14 bg-background border-b border-border flex items-center justify-between px-6 sticky top-0 z-30` (adds missing `border-b border-border` per F-03)
   - In header, between the SeatKit wordmark Link and AppPresenceBadgeRow, add a flex container with ThemeToggle and AppPresenceBadgeRow:
     ```
     <div className="flex items-center gap-2">
       <ThemeToggle />
       <AppPresenceBadgeRow />
     </div>
     ```
   - Add `<Toaster toastOptions={{ style: { background: 'hsl(var(--foreground))', color: 'hsl(var(--background))', border: '1px solid hsl(var(--border))' } }} />` before closing `</ThemeProvider>` (inverted colors for toast per DESIGN.md undo toast spec)

4. Replace `packages/web/src/app/page.tsx` (per D-05, D-17/F-08):
   - Remove entire current content
   - Replace with:
     ```typescript
     import { redirect } from 'next/navigation';

     export default function Home() {
       redirect('/reservations');
     }
     ```
  </action>
  <verify>
    <automated>cd /workspace/packages/web && grep -q "suppressHydrationWarning" src/app/layout.tsx && grep -q "ThemeProvider" src/app/layout.tsx && grep -q "ThemeToggle" src/app/layout.tsx && grep -q "border-b border-border" src/app/layout.tsx && grep -q "Toaster" src/app/layout.tsx && grep -q "defaultTheme.*dark" src/app/layout.tsx && grep -q "redirect" src/app/page.tsx && grep -q "useTheme" src/components/theme-toggle.tsx && pnpm exec tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - packages/web/src/app/layout.tsx contains `suppressHydrationWarning` on html tag
    - packages/web/src/app/layout.tsx contains `<ThemeProvider` with `attribute="class"` and `defaultTheme="dark"` and `storageKey="seatkit-theme"`
    - packages/web/src/app/layout.tsx header has `border-b border-border` in className
    - packages/web/src/app/layout.tsx contains `<ThemeToggle` rendered in nav
    - packages/web/src/app/layout.tsx contains `<Toaster` component
    - packages/web/src/app/page.tsx contains `import { redirect } from 'next/navigation'` and `redirect('/reservations')`
    - packages/web/src/app/page.tsx does NOT contain `text-gray-600` or `bg-blue-600`
    - packages/web/src/components/theme-toggle.tsx exports `ThemeToggle` function
    - packages/web/src/components/theme-toggle.tsx contains `useTheme` import from `next-themes`
    - packages/web/src/components/theme-toggle.tsx contains mounted state guard with `useEffect`
    - packages/web/src/components/theme-provider.tsx exports `ThemeProvider` function
    - TypeScript compilation produces no errors
  </acceptance_criteria>
  <done>Dark mode defaults to dark on first visit. ThemeToggle in nav cycles system/dark/light with persisted preference. Home page redirects to /reservations. Nav has border-b. Sonner Toaster mounted globally.</done>
</task>

<task type="auto">
  <name>Task 3: Fix hardcoded color tokens on ErrorBoundary, ConflictModal, PresenceBadge</name>
  <files>packages/web/src/components/error-boundary.tsx, packages/web/src/components/conflict-modal.tsx, packages/web/src/components/presence-badge.tsx</files>
  <read_first>
    - packages/web/src/components/error-boundary.tsx
    - packages/web/src/components/conflict-modal.tsx
    - packages/web/src/components/presence-badge.tsx
    - .planning/phases/05.2-ui-review-polish/05.2-UI-SPEC.md (F-01 table, Amber Accent table, Copywriting Contract)
    - DESIGN.md (Section 2: Color Palette, Section 4: Component Stylings)
  </read_first>
  <action>
1. Fix `packages/web/src/components/error-boundary.tsx` (per D-10/F-01):
   - Line 57: `text-red-600` -> `text-destructive`
   - Line 60: `text-gray-600` -> `text-muted-foreground`
   - Line 65: `bg-blue-600 text-white rounded-lg hover:bg-blue-700` -> `bg-primary text-primary-foreground rounded-md hover:bg-primary/90 focus:outline-none focus:ring-2 focus:ring-ring focus:ring-offset-2`
   - Update heading text to match copywriting contract: "Something went wrong"
   - Update button text to match copywriting contract: "Try again"
   - Add `role="alert"` to the error container div

2. Fix `packages/web/src/components/conflict-modal.tsx` (per D-10/F-01, D-13/F-04):
   - Line 76: `text-gray-900` -> `text-foreground`
   - Line 79: `text-gray-600` -> `text-muted-foreground`
   - Line 84: `text-gray-500 italic` -> `text-muted-foreground italic`
   - Line 88: `text-gray-500` (thead) -> `text-muted-foreground`
   - Line 96: `border-gray-100` -> `border-border`
   - Line 97: `text-gray-700` -> `text-foreground`
   - Line 98: `text-amber-700` -> `text-amber-400` (draft value highlight -- amber is one of the 5 reserved uses)
   - Line 99: `text-gray-600` -> `text-muted-foreground`
   - Line 110: `text-gray-700 border border-gray-300 rounded-md hover:bg-gray-50` -> `text-foreground border border-border rounded-md hover:bg-muted focus:outline-none focus:ring-2 focus:ring-ring focus:ring-offset-2`
   - Line 117: Keep `bg-amber-600 rounded-md hover:bg-amber-700` (conflict CTA is reserved amber use #3) but add `text-white focus:outline-none focus:ring-2 focus:ring-ring focus:ring-offset-2`
   - Update heading to match copywriting: "Edit conflict"
   - Update body to match copywriting: "Another staff member saved changes while you were editing. Review the differences below."
   - Update button labels: "Discard draft" and "Apply your changes on top"

3. Fix `packages/web/src/components/presence-badge.tsx` (per D-10/F-01):
   - Line 31: viewing state `bg-gray-200 text-gray-600 ring-2 ring-gray-300` -> `bg-muted text-muted-foreground ring-2 ring-border`
   - Keep editing state as-is: `bg-amber-400 text-amber-900 ring-2 ring-amber-500` (reserved amber use #1)
  </action>
  <verify>
    <automated>cd /workspace/packages/web && ! grep -n "text-gray-\|bg-gray-\|text-red-\|bg-blue-\|border-gray-\|ring-gray-\|hover:bg-gray-\|text-amber-700" src/components/error-boundary.tsx src/components/conflict-modal.tsx src/components/presence-badge.tsx && grep -q "text-destructive" src/components/error-boundary.tsx && grep -q "bg-primary" src/components/error-boundary.tsx && grep -q "text-foreground" src/components/conflict-modal.tsx && grep -q "border-border" src/components/conflict-modal.tsx && grep -q "bg-muted text-muted-foreground" src/components/presence-badge.tsx && pnpm exec tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - packages/web/src/components/error-boundary.tsx contains NO instances of `text-red-`, `text-gray-`, `bg-blue-`, `hover:bg-blue-`
    - packages/web/src/components/error-boundary.tsx contains `text-destructive` for the error heading
    - packages/web/src/components/error-boundary.tsx contains `bg-primary text-primary-foreground` for the retry button
    - packages/web/src/components/error-boundary.tsx contains `role="alert"` on the error container
    - packages/web/src/components/conflict-modal.tsx contains NO instances of `text-gray-`, `border-gray-`, `hover:bg-gray-`, `ring-gray-`, `text-amber-700`
    - packages/web/src/components/conflict-modal.tsx contains `text-foreground` for heading
    - packages/web/src/components/conflict-modal.tsx contains `border-border` for table row borders
    - packages/web/src/components/conflict-modal.tsx buttons have `focus:ring-2 focus:ring-ring` focus indicators
    - packages/web/src/components/conflict-modal.tsx KEEPS `bg-amber-600` on the Apply button (reserved amber use)
    - packages/web/src/components/presence-badge.tsx viewing state uses `bg-muted text-muted-foreground ring-2 ring-border`
    - packages/web/src/components/presence-badge.tsx editing state KEEPS `bg-amber-400 text-amber-900 ring-2 ring-amber-500`
    - TypeScript compilation produces no errors
  </acceptance_criteria>
  <done>All hardcoded Tailwind color classes replaced with CSS variable design tokens on ErrorBoundary, ConflictModal, and PresenceBadge. Amber reserved uses preserved. Focus rings added to ConflictModal buttons. Copywriting matches UI-SPEC contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage theme key | Theme preference stored in browser localStorage via next-themes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.2-01 | Information Disclosure | localStorage `seatkit-theme` key | accept | Theme preference (dark/light/system) is not sensitive data. No PII. Standard browser API. |
| T-05.2-02 | Tampering | Copy-paste UI components (Magic UI, Aceternity) | mitigate | Review source for `eval`, `fetch`, `innerHTML`, `dangerouslySetInnerHTML` before inclusion. UI-SPEC confirms clean review. Applied in Plan 04. |
</threat_model>

<verification>
1. `cd packages/web && pnpm exec tsc --noEmit` -- zero errors
2. `cd packages/web && pnpm test` -- existing tests pass
3. `grep -rn "text-gray-\|bg-gray-\|text-red-\|bg-blue-\|border-gray-\|ring-gray-\|hover:bg-gray-\|hover:bg-blue-" packages/web/src/components/error-boundary.tsx packages/web/src/components/conflict-modal.tsx packages/web/src/components/presence-badge.tsx packages/web/src/app/page.tsx` -- zero matches
4. `grep -q "darkMode" packages/web/tailwind.config.ts` -- present
5. `grep -q "ThemeProvider" packages/web/src/app/layout.tsx` -- present
6. `grep -q "redirect" packages/web/src/app/page.tsx` -- present
</verification>

<success_criteria>
- Dark mode infrastructure fully operational: Tailwind darkMode config, ThemeProvider, ThemeToggle, defaultTheme=dark
- shadcn CLI initialized with 5 components installed (sheet, dialog, skeleton, tabs, sonner)
- Home page redirects to /reservations (no placeholder page)
- Nav bar has border-b border-border
- Sonner Toaster mounted in layout
- ErrorBoundary, ConflictModal, PresenceBadge use only design tokens (no hardcoded gray/blue/red classes)
- All existing tests pass, TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/05.2-ui-review-polish/05.2-01-SUMMARY.md`
</output>
