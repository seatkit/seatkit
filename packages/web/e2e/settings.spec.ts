/**
 * Settings E2E tests — Phase 2 Wave 0 stubs
 * Task IDs: 2-e2e-04, 2-e2e-05 (see 02-VALIDATION.md)
 */

import { test } from '@playwright/test';

test.describe('Settings pages', () => {
	test.fixme('admin adds table via form; table appears in list (2-e2e-04)', async () => {});
	test.fixme('drag-to-reorder priority list auto-saves on drop (2-e2e-05)', async () => {});
});
