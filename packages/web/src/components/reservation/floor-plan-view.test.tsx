/**
 * Wave 0 test stubs — TABLE-07
 */
import { describe, it } from 'vitest';

describe('FloorPlanView (TABLE-07)', () => {
	it.todo('renders a card for each table positioned by positionRow and positionCol');
	it.todo('tables belonging to the same reservation have ring-2 in UUID-derived color');
	it.todo('tables with no reservation have default card styling with no ring');
	it.todo('empty state shown when no tables are configured');
});
