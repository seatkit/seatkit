/**
 * Wave 0 test stubs — VIEW-03, VIEW-04, VIEW-05, VIEW-06
 */
import { describe, it } from 'vitest';

describe('ReservationListView (VIEW-03 through VIEW-06)', () => {
	it.todo('search input filters reservations by customer.name (case insensitive, debounced 300ms)');
	it.todo('status filter chip shows only matching reservations');
	it.todo('deleted filter chip shows soft-deleted reservations with Recover button');
	it.todo('sort by time shows earliest reservation first');
	it.todo('sort by guest name shows alphabetical order');
	it.todo('group by day renders sticky group header with date label');
	it.todo('group by week renders sticky group header with week range');
	it.todo('empty state shown when no reservations match active filters');
});
